// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetAgentBudgetArgs {
    pub agent_id: String,
    pub total_budget_tokens: u64,
    pub total_budget_usd: String,
    pub max_single_request_tokens: u64,
    pub updated_at: String,
}

impl From<SetAgentBudgetArgs> for super::Reducer {
    fn from(args: SetAgentBudgetArgs) -> Self {
        Self::SetAgentBudget {
            agent_id: args.agent_id,
            total_budget_tokens: args.total_budget_tokens,
            total_budget_usd: args.total_budget_usd,
            max_single_request_tokens: args.max_single_request_tokens,
            updated_at: args.updated_at,
        }
    }
}

impl __sdk::InModule for SetAgentBudgetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_agent_budget`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_agent_budget {
    /// Request that the remote module invoke the reducer `set_agent_budget` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_agent_budget:set_agent_budget_then`] to run a callback after the reducer completes.
    fn set_agent_budget(
        &self,
        agent_id: String,
        total_budget_tokens: u64,
        total_budget_usd: String,
        max_single_request_tokens: u64,
        updated_at: String,
    ) -> __sdk::Result<()> {
        self.set_agent_budget_then(
            agent_id,
            total_budget_tokens,
            total_budget_usd,
            max_single_request_tokens,
            updated_at,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `set_agent_budget` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_agent_budget_then(
        &self,
        agent_id: String,
        total_budget_tokens: u64,
        total_budget_usd: String,
        max_single_request_tokens: u64,
        updated_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_agent_budget for super::RemoteReducers {
    fn set_agent_budget_then(
        &self,
        agent_id: String,
        total_budget_tokens: u64,
        total_budget_usd: String,
        max_single_request_tokens: u64,
        updated_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetAgentBudgetArgs {
                agent_id,
                total_budget_tokens,
                total_budget_usd,
                max_single_request_tokens,
                updated_at,
            },
            callback,
        )
    }
}
