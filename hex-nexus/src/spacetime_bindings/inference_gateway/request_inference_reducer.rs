// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RequestInferenceArgs {
    pub agent_id: String,
    pub provider: String,
    pub model: String,
    pub messages_json: String,
    pub tools_json: String,
    pub max_tokens: u32,
    pub temperature: String,
    pub thinking_budget: u32,
    pub cache_control: u8,
    pub priority: u8,
    pub created_at: String,
}

impl From<RequestInferenceArgs> for super::Reducer {
    fn from(args: RequestInferenceArgs) -> Self {
        Self::RequestInference {
            agent_id: args.agent_id,
            provider: args.provider,
            model: args.model,
            messages_json: args.messages_json,
            tools_json: args.tools_json,
            max_tokens: args.max_tokens,
            temperature: args.temperature,
            thinking_budget: args.thinking_budget,
            cache_control: args.cache_control,
            priority: args.priority,
            created_at: args.created_at,
        }
    }
}

impl __sdk::InModule for RequestInferenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `request_inference`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait request_inference {
    /// Request that the remote module invoke the reducer `request_inference` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`request_inference:request_inference_then`] to run a callback after the reducer completes.
    fn request_inference(
        &self,
        agent_id: String,
        provider: String,
        model: String,
        messages_json: String,
        tools_json: String,
        max_tokens: u32,
        temperature: String,
        thinking_budget: u32,
        cache_control: u8,
        priority: u8,
        created_at: String,
    ) -> __sdk::Result<()> {
        self.request_inference_then(
            agent_id,
            provider,
            model,
            messages_json,
            tools_json,
            max_tokens,
            temperature,
            thinking_budget,
            cache_control,
            priority,
            created_at,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `request_inference` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn request_inference_then(
        &self,
        agent_id: String,
        provider: String,
        model: String,
        messages_json: String,
        tools_json: String,
        max_tokens: u32,
        temperature: String,
        thinking_budget: u32,
        cache_control: u8,
        priority: u8,
        created_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl request_inference for super::RemoteReducers {
    fn request_inference_then(
        &self,
        agent_id: String,
        provider: String,
        model: String,
        messages_json: String,
        tools_json: String,
        max_tokens: u32,
        temperature: String,
        thinking_budget: u32,
        cache_control: u8,
        priority: u8,
        created_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RequestInferenceArgs {
                agent_id,
                provider,
                model,
                messages_json,
                tools_json,
                max_tokens,
                temperature,
                thinking_budget,
                cache_control,
                priority,
                created_at,
            },
            callback,
        )
    }
}
