// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ToggleHookArgs {
    pub id: String,
    pub enabled: bool,
    pub timestamp: String,
}

impl From<ToggleHookArgs> for super::Reducer {
    fn from(args: ToggleHookArgs) -> Self {
        Self::ToggleHook {
            id: args.id,
            enabled: args.enabled,
            timestamp: args.timestamp,
        }
    }
}

impl __sdk::InModule for ToggleHookArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `toggle_hook`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait toggle_hook {
    /// Request that the remote module invoke the reducer `toggle_hook` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`toggle_hook:toggle_hook_then`] to run a callback after the reducer completes.
    fn toggle_hook(&self, id: String, enabled: bool, timestamp: String) -> __sdk::Result<()> {
        self.toggle_hook_then(id, enabled, timestamp, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `toggle_hook` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn toggle_hook_then(
        &self,
        id: String,
        enabled: bool,
        timestamp: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl toggle_hook for super::RemoteReducers {
    fn toggle_hook_then(
        &self,
        id: String,
        enabled: bool,
        timestamp: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ToggleHookArgs {
                id,
                enabled,
                timestamp,
            },
            callback,
        )
    }
}
