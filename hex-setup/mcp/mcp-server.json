{
  "mcpServers": {
    "hex": {
      "command": "hex",
      "args": ["mcp"],
      "env": {},
      "description": "hex AAIDE — architecture enforcement, swarm coordination, ADR management, workplan execution"
    }
  }
}
