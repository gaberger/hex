Access swarm-scoped memory for the current swarm.

Use for coordination data within the active swarm: task assignments, agent decisions, phase progress, and inter-agent communication. Key-value store scoped to 'swarm:{id}' level. Memory is available to all agents in the swarm.
