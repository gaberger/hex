Access agent-scoped memory for this agent's personal state.

Use for personal progress tracking, intermediate results, and agent-specific state. Key-value store scoped to 'agent:{id}' level. This memory is private to the current agent instance.
