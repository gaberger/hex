#!/usr/bin/env bash
# Start script for Plan: build a todo REST API in Rust with axum: POST /todos to create, GET /todos to list, DELETE /todos/:id to remove. In-memory storage, JSON responses.
# Generated by hex dev

set -euo pipefail
cd "$(dirname "$0")"

echo "Building..."
cargo build --release

echo "Starting application..."
./target/release/$(basename $(pwd))
