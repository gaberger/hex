#!/usr/bin/env bash
# Start script for In-Memory Todo API with Hexagonal Architecture
# Generated by hex dev

set -euo pipefail
cd "$(dirname "$0")"

echo "Building..."
cargo build --release

echo "Starting application..."
./target/release/$(basename $(pwd))
