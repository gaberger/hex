\* Buggy-daemon configuration: DispatchVacuous transitions are reachable,
\* and TimeoutSweep has no weak-fairness guarantee. Expect TLC to report
\* a liveness violation on TerminalReachable.
SPECIFICATION Spec
CONSTANTS
    Tasks = {t1}
    TimeoutTicks = 2
    GraceTicks = 1
INVARIANTS
    TypeOK
    HandleInvariant
PROPERTIES
    TerminalReachable
