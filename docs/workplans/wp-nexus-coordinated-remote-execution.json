{
  "adr": "ADR-2604121630",
  "created_at": "2026-04-12T16:30:00Z",
  "created_by": "human",
  "description": "Remote agents poll nexus for workplan tasks, execute locally with ADR-005 gates, report results to SpacetimeDB. Enables hex plan report for all executions and cross-agent RL learning.",
  "feature": "Nexus-Coordinated Remote Agent Execution",
  "id": "wp-nexus-coordinated-remote-execution",
  "phases": [
    {
      "description": "hex agent worker polls nexus for tasks, executes locally, reports back",
      "gate": {
        "blocking": true,
        "command": "cargo test -p hex-cli worker && cargo check --workspace",
        "type": "test"
      },
      "id": "P1",
      "name": "Worker poll loop",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "New GET endpoint in hex-nexus/src/routes/swarms.rs. Accepts agent_id and role query params. Returns the next unassigned task matching the agent's role, atomically assigns it (CAS). Returns 204 No Content when no tasks available.",
          "files": [
            "hex-nexus/src/routes/swarms.rs"
          ],
          "id": "P1.1",
          "layer": "primary",
          "name": "Add /api/hexflo/tasks/poll endpoint",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P1.1"
          ],
          "description": "New subcommand in hex-cli/src/commands/agent.rs. Connects to nexus, registers agent, enters poll loop (2s interval). On task receipt: run Ollama inference with tier model, execute ADR-005 gate pipeline (compile + test + retry), PATCH task result back to nexus, POST RL reward. --once flag for single-task mode (testing). --role flag for agent specialization.",
          "files": [
            "hex-cli/src/commands/agent.rs"
          ],
          "id": "P1.2",
          "layer": "primary",
          "name": "Implement hex agent worker command",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P1.2"
          ],
          "description": "Tests: (a) worker polls and receives task, (b) worker executes and reports success, (c) worker retries on compile failure, (d) worker escalates after max iterations, (e) --once mode exits after one task. Mock the nexus API with httpmock.",
          "files": [
            "hex-cli/tests/worker.rs"
          ],
          "id": "P1.3",
          "layer": "primary",
          "name": "Worker unit tests",
          "status": "done"
        },
        {
          "agent": "integrator",
          "deps": [
            "P1.2"
          ],
          "description": "SSH to bazzite, start hex agent worker --once --nexus http://localhost:5556, create a T2 HexFlo task on coordinator, verify worker picks it up, generates code, reports result back. Assert: task status=completed in SpacetimeDB, generated code in result field.",
          "files": [
            "docs/analysis/wp-nexus-coord-p1.4-bazzite-e2e.md"
          ],
          "id": "P1.4",
          "layer": "primary",
          "name": "E2E: worker on bazzite executes one task",
          "status": "done"
        }
      ],
      "tier": 1
    },
    {
      "description": "hex plan execute registers in SpacetimeDB and delegates to workers",
      "gate": {
        "blocking": true,
        "command": "cargo test -p hex-nexus workplan && cargo check --workspace",
        "type": "test"
      },
      "id": "P2",
      "name": "Coordinator dispatch",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "When nexus is reachable, hex plan execute POSTs to /api/workplan/execute which creates a workplan_execution record in SpacetimeDB with id, workplan_path, status=running, started_at. Each task becomes a HexFlo swarm task assigned by tier\u2192agent role mapping.",
          "files": [
            "hex-nexus/src/routes/orchestration.rs",
            "hex-cli/src/commands/plan.rs"
          ],
          "id": "P2.1",
          "layer": "primary",
          "name": "Register execution in SpacetimeDB from hex plan execute",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P2.1"
          ],
          "description": "After creating tasks for a phase, coordinator subscribes to SpacetimeDB task completion events. When all tasks in a phase are completed, runs the phase gate command. If gate passes, advances to next phase. If gate fails, marks execution as failed with gate output.",
          "files": [
            "hex-nexus/src/orchestration/workplan_executor.rs"
          ],
          "id": "P2.2",
          "layer": "primary",
          "name": "Phase gate monitoring via SpacetimeDB",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P2.1"
          ],
          "description": "If a worker reports task failure (all iterations exhausted), coordinator checks for other available agents. If a higher-tier model is available on another agent, reassign. If no agents available, mark task as escalate and notify via inbox.",
          "files": [
            "hex-nexus/src/orchestration/workplan_executor.rs"
          ],
          "id": "P2.3",
          "layer": "primary",
          "name": "Task reassignment on worker failure",
          "status": "done"
        }
      ],
      "tier": 2
    },
    {
      "description": "hex plan report reads from SpacetimeDB, RL rewards aggregate across agents",
      "gate": {
        "blocking": true,
        "command": "cargo check --workspace",
        "type": "test"
      },
      "id": "P3",
      "name": "Reporting and RL aggregation",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "Update hex plan report to query SpacetimeDB for execution records. Show: phases, tasks with agent assignments, gate results (pass/fail + error), iteration count, token usage, duration. Works for both local and remote executions.",
          "files": [
            "hex-cli/src/commands/plan.rs"
          ],
          "id": "P3.1",
          "layer": "primary",
          "name": "hex plan report reads from SpacetimeDB",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "Workers POST RL rewards to coordinator nexus which forwards to SpacetimeDB rl-engine. The coordinator's Q-table accumulates rewards from all agents. Add hex inference rl-report command showing Q-values, visit counts, and per-agent contribution.",
          "files": [
            "hex-nexus/src/routes/inference.rs",
            "hex-cli/src/commands/inference.rs"
          ],
          "id": "P3.2",
          "layer": "secondary",
          "name": "Cross-agent RL reward aggregation",
          "status": "done"
        },
        {
          "agent": "integrator",
          "deps": [
            "P3.1",
            "P3.2"
          ],
          "description": "Full end-to-end: hex plan execute on coordinator, bazzite worker executes tasks, coordinator generates report. Verify: SpacetimeDB has execution + task records, hex plan report shows correct data, RL Q-table has entries from remote agent.",
          "files": [
            "docs/analysis/wp-nexus-coord-p3.3-coordinated-workplan-report.md"
          ],
          "id": "P3.3",
          "layer": "primary",
          "name": "E2E: coordinated workplan on bazzite with report",
          "status": "done"
        }
      ],
      "tier": 3
    }
  ],
  "priority": "high",
  "relates_to": "wp-tiered-inference-routing",
  "status": "done"
}