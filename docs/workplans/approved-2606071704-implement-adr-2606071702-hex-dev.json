{
  "adr": "ADR-2026-04-11-0227",
  "created_at": "2026-06-07T17:04:24.773701797-04:00",
  "id": "draft-2606071704-implement-adr-2606071702-hex-dev",
  "kind": "workplan-draft",
  "next_steps": [
    "Run /hex-feature-dev to expand this draft into a full workplan",
    "Or run `hex plan drafts approve draft-2606071704-implement-adr-2606071702-hex-dev.json`",
    "Or run `hex plan drafts clear --name draft-2606071704-implement-adr-2606071702-hex-dev`"
  ],
  "notes": "This is a draft created by ADR-2026-04-11-0227 auto-invoke. It contains only the original prompt — no specs, steps, or tiers have been generated yet. The planner agent will fill these in when the draft is picked up.",
  "origin": "auto-invoke",
  "prompt": "Implement ADR-2606071702: hex dev deploy verb + installed-binary freshness gate. (1) Add DevAction::Deploy (hex dev deploy) in hex-cli's dev command module: enumerate the current workspace binaries (hex-cli, hex-nexus, hex-agent), run cargo build --release for each, install them to the resolved BIN_DIR using the same prefix logic as scripts/install.sh (~/.local/bin or $PREFIX), then restart managed services unless --no-restart is passed. Add a --check flag that reports what would be deployed and the current drift without building. (2) Add hex-cli/build.rs that embeds GIT_BUILD_HASH at compile time (mirror hex-nexus) and expose 'hex --build-hash'. (3) Add a shared freshness helper that compares the installed --build-hash for hex and hex-nexus against git rev-parse of the last commit touching each crate; wire it into hex doctor as a check AND append a non-fatal warning to the hex dev validate summary ('validated OK but installed hex is N commits behind source; run hex dev deploy'). (4) Deprecate scripts/rebuild.sh with a pointer to hex dev deploy. Tests: --check reports drift on a deliberately-stale install; build.rs hash present and surfaced by --build-hash; freshness helper classifies behind/current correctly. validate stays a correctness gate and must NOT auto-deploy.",
  "status": "pending-planner"
}