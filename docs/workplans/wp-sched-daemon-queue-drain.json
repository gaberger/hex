{
  "id": "wp-sched-daemon-queue-drain",
  "phases": [
    {
      "id": "P1",
      "tasks": [
        {
          "evidence": [
            "grep -r 'queue.*drain\\|process.*pending\\|dequeue' hex-cli/src hex-nexus/src --include='*.rs' | grep -v '//' | head -5",
            "cargo check --workspace"
          ],
          "files": [
            "hex-cli/src/commands/sched/**/*.rs",
            "hex-nexus/src/routes/sched.rs"
          ],
          "id": "P1.1",
          "status": "done",
          "strategy_hint": "inference",
          "title": "Find hex sched daemon implementation and add queue drain logic that: (1) fetches pending tasks from queue, (2) executes workplans via hex-agent, (3) updates task status to completed/failed, (4) writes detailed failure reasons to task result field"
        }
      ],
      "title": "Implement queue drain logic"
    }
  ],
  "title": "Fix sched daemon to actually drain the queue"
}