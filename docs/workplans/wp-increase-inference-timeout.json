{
  "id": "wp-increase-inference-timeout",
  "title": "Increase inference timeout to 300s for large models",
  "phases": [
    {
      "id": "P1",
      "title": "Update timeout constant",
      "tasks": [
        {
          "id": "P1.1",
          "title": "Change INFERENCE_TIMEOUT in hex-agent/src/inference_client.rs from Duration::from_secs(30) to Duration::from_secs(300) to allow large models like qwen2.5-coder:32b enough time",
          "status": "failed",
          "strategy_hint": "transform",
          "files": [
            "hex-agent/src/inference_client.rs"
          ],
          "evidence": [
            "grep -q 'INFERENCE_TIMEOUT.*300' hex-agent/src/inference_client.rs",
            "cargo check -p hex-agent"
          ]
        }
      ]
    }
  ]
}