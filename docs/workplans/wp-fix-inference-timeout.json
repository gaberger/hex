{
  "id": "wp-fix-inference-timeout",
  "title": "Investigate and fix inference timeouts in workplan execution",
  "phases": [
    {
      "id": "P1",
      "title": "Diagnose timeout root cause",
      "tasks": [
        {
          "id": "P1.1",
          "title": "Check Ollama service status, verify models are loaded, check if inference endpoints are responding, add debug logging to hex-agent inference_client.rs to capture timeout details",
          "status": "failed",
          "strategy_hint": "inference",
          "files": [
            "hex-agent/src/inference_client.rs"
          ],
          "evidence": [
            "grep -q 'timeout.*debug\\|inference.*timeout' hex-agent/src/inference_client.rs",
            "cargo check -p hex-agent"
          ]
        },
        {
          "id": "P1.2",
          "title": "Increase inference timeout from 120s to 300s in hex-agent/src/inference_client.rs if models are slow, OR fix connection issues if Ollama is unreachable",
          "status": "pending",
          "strategy_hint": "codegen",
          "files": [
            "hex-agent/src/inference_client.rs"
          ],
          "evidence": [
            "grep -q '300\\|timeout.*secs' hex-agent/src/inference_client.rs",
            "cargo check -p hex-agent"
          ]
        }
      ]
    }
  ]
}