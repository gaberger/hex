{
  "adr": "ADR-2604120202",
  "created_at": "2026-04-12T02:02:00Z",
  "created_by": "human",
  "description": "Route T1/T2 tasks to local models with scaffolding (Best-of-N + compile gate + GBNF grammars + retry loop + cascading escalation), dropping frontier API calls to ~30%. Builds on ADR-2604112000 standalone dispatch + the wired inference router.",
  "feature": "Tiered Inference Routing with Local Model Scaffolding",
  "id": "wp-tiered-inference-routing",
  "phases": [
    {
      "description": "Add TaskTier enum to CodeGenRequest, wire the router to select models per tier, make the mapping configurable via .hex/project.json. This is the foundation everything else builds on.",
      "gate": {
        "blocking": true,
        "command": "cargo check --workspace",
        "type": "typecheck"
      },
      "id": "P1",
      "name": "Task-tier field + tier-aware routing",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "Add TaskTier { T1, T2, T2_5, T3 } to hex-core/src/domain/ or hex-nexus/src/remote/transport.rs (wherever CodeGenRequest lives). Add tier: Option<TaskTier> field to CodeGenRequest. Default None means 'unclassified — use model from request'. Serialize as snake_case strings.",
          "files": [
            "hex-nexus/src/remote/transport.rs"
          ],
          "id": "P1.1",
          "layer": "domain",
          "name": "Define TaskTier enum + add to CodeGenRequest",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P1.1"
          ],
          "description": "In InferenceRouterAdapter::route_request, before select_server, check request.tier. If Some(tier), override the model name from the tier→model mapping. The mapping is a struct TierModelConfig { t1: String, t2: String, t2_5: String, t3: String } loaded from .hex/project.json inference.tier_models (with hardcoded defaults: t1=qwen3:4b, t2=qwen2.5-coder:32b, t2_5=devstral-small-2:24b, t3=claude). If tier is T3 and no frontier adapter is available, return Err with remediation hint.",
          "files": [
            "hex-nexus/src/adapters/inference_router.rs",
            "hex-nexus/src/adapters/inference_router/tier_config.rs"
          ],
          "id": "P1.2",
          "layer": "secondary",
          "name": "Tier-aware model selection in inference_router.rs",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P1.1"
          ],
          "description": "In the workplan executor's dispatch path, populate CodeGenRequest.tier from the workplan task's layer + complexity heuristic. Simple mapping: layer=domain|ports → T2, layer=primary|secondary with deps.len() < 2 → T2, with deps.len() >= 2 → T2.5. Tasks tagged agent=planner → T2. The hook route classifier already handles T1/T2/T3 for interactive prompts — reuse that logic. Do not overthink the heuristic; escalation tracking (P4) will refine it.",
          "files": [
            "hex-nexus/src/orchestration/workplan_executor.rs"
          ],
          "id": "P1.3",
          "layer": "primary",
          "name": "Wire tier from workplan task to CodeGenRequest",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P1.2"
          ],
          "description": "Tests: (a) T1 request selects qwen3:4b model override, (b) T2 selects qwen2.5-coder:32b, (c) T3 with no frontier returns error, (d) None tier preserves original model from request, (e) custom tier_models config overrides defaults. Use MockInferencePort + httpmock.",
          "files": [
            "hex-nexus/tests/tier_routing.rs"
          ],
          "id": "P1.4",
          "layer": "primary",
          "name": "Unit tests for tier routing",
          "status": "done"
        }
      ],
      "tier": 0
    },
    {
      "description": "Restructure agent YAML prompt templates so the first ~800 tokens are identical across all agents of the same role. Flatten nested instructions for quantization-aware prompting. No Rust code changes — YAML and markdown template edits only.",
      "gate": {
        "blocking": true,
        "command": "hex analyze . && cargo check --workspace",
        "type": "lint"
      },
      "id": "P2",
      "name": "KV-cache prefix sharing + prompt restructuring",
      "tasks": [
        {
          "agent": "planner",
          "deps": [],
          "description": "Read all 14 agent YAMLs in hex-cli/assets/agents/hex/hex/. Identify the common prefix tokens (role, architecture rules, output format, tool usage). Document in docs/analysis/prompt-prefix-audit.md which tokens are shared vs unique per role. Target: 800 shared tokens that can be KV-cached.",
          "files": [
            "docs/analysis/prompt-prefix-audit.md"
          ],
          "id": "P2.1",
          "layer": "ports",
          "name": "Audit current agent prompt templates for shared prefix",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P2.1"
          ],
          "description": "Edit the 14 agent YAMLs to: (a) move shared context to a common prefix section, (b) flatten nested instructions to numbered steps, (c) front-load file/code context before instructions, (d) repeat key constraints at end of prompt ('REMEMBER: only edit files in your manifest'). ADR-2604111142 content rule applies — no hex-intf specifics in embedded assets.",
          "files": [
            "hex-cli/assets/agents/hex/hex/hex-coder.yml",
            "hex-cli/assets/agents/hex/hex/planner.yml",
            "hex-cli/assets/agents/hex/hex/integrator.yml"
          ],
          "id": "P2.2",
          "layer": "secondary",
          "name": "Restructure agent YAMLs for prefix sharing + Q4 optimization",
          "status": "done"
        }
      ],
      "tier": 0
    },
    {
      "description": "New orchestration component that generates multiple completions, runs cargo check on each, returns the first that compiles, and retries with error feedback on failure. The core quality-improvement mechanism.",
      "gate": {
        "blocking": true,
        "command": "cargo test -p hex-nexus scaffolding",
        "type": "test"
      },
      "id": "P3",
      "name": "Scaffolded dispatch — Best-of-N + compile gate + retry loop",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "New file hex-nexus/src/orchestration/scaffolding.rs. ScaffoldedDispatch wraps an Arc<dyn IInferencePort> and adds: generate N completions → extract code from each → run compile_check(code) → return first that passes. N is configurable per tier (T1:1, T2:3, T2.5:5). compile_check() shells out to 'cargo check' (or a configurable command per project language). Uses tokio::process::Command, captures stderr on failure for the retry loop.",
          "files": [
            "hex-nexus/src/orchestration/scaffolding.rs",
            "hex-nexus/src/orchestration/mod.rs"
          ],
          "id": "P3.1",
          "layer": "primary",
          "name": "ScaffoldedDispatch struct + Best-of-N with compile gate",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P3.1"
          ],
          "description": "If all N completions fail compile_check, take the best error (shortest stderr) and append it to the original prompt: 'The previous attempt produced this compiler error: {stderr}. Fix the code.' Retry up to 2 times. Total attempts = N * (1 + max_retries). If still failing, return CompileGateFailed { attempts, best_error } for escalation.",
          "files": [
            "hex-nexus/src/orchestration/scaffolding.rs"
          ],
          "id": "P3.2",
          "layer": "primary",
          "name": "Error-feedback retry loop",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "Add grammar: Option<String> to InferenceRequest (or to OllamaInferenceAdapter::complete's internal request body). When set, pass it as the 'grammar' field in the /api/generate JSON body — Ollama supports this natively via llama.cpp GBNF. Define three built-in grammars as const strings: CODE_ONLY (just a code block), CODE_AND_COMMIT (JSON with files, code, commit_msg), ANALYSIS (structured markdown). Grammar selection is driven by the agent role.",
          "files": [
            "hex-nexus/src/adapters/inference/ollama.rs",
            "hex-nexus/src/orchestration/grammars.rs"
          ],
          "id": "P3.3",
          "layer": "secondary",
          "name": "GBNF grammar support in OllamaInferenceAdapter",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P3.1",
            "P3.2",
            "P3.3"
          ],
          "description": "Hermetic tests: (a) Best-of-3 where second attempt compiles → returns second, (b) all 3 fail → retry with error → succeeds on retry 1, (c) all fail all retries → CompileGateFailed with attempt count, (d) grammar is forwarded to adapter, (e) N=1 for T1 → no Best-of-N overhead. Mock the compile_check with a closure that accepts/rejects based on output content. No real cargo check in tests.",
          "files": [
            "hex-nexus/tests/scaffolding.rs"
          ],
          "id": "P3.4",
          "layer": "primary",
          "name": "Scaffolding tests — mocked compile gate",
          "status": "done"
        }
      ],
      "tier": 2
    },
    {
      "description": "When scaffolded dispatch fails, escalate to frontier. Track escalation rates per task-type in HexFlo memory to refine tier boundaries over time.",
      "gate": {
        "blocking": true,
        "command": "cargo test -p hex-nexus scaffolding::escalation",
        "type": "test"
      },
      "id": "P4",
      "name": "Cascading escalation + tracking",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "When CompileGateFailed, check if a frontier adapter is available (ClaudeCodeInferenceAdapter or an Anthropic/OpenAI cloud adapter). If available, re-dispatch to frontier with the original prompt (not the retry-augmented one). Log the escalation to tracing::warn with structured fields. If no frontier available, return the CompileGateFailed error to the caller.",
          "files": [
            "hex-nexus/src/orchestration/scaffolding.rs"
          ],
          "id": "P4.1",
          "layer": "primary",
          "name": "Escalation path in ScaffoldedDispatch",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P4.1"
          ],
          "description": "On each escalation, write to HexFlo memory: key=escalation:{task_type}:{model}, value={count, last_escalated, sample_error}. On each successful local dispatch, write success:{task_type}:{model}. Expose a hex inference escalation-report CLI command that reads these keys and prints a table of task-types with escalation rates. If any task-type exceeds 50% escalation rate, print a recommendation to reclassify it as a higher tier.",
          "files": [
            "hex-nexus/src/orchestration/scaffolding.rs",
            "hex-cli/src/commands/inference.rs"
          ],
          "id": "P4.2",
          "layer": "primary",
          "name": "Escalation tracking in HexFlo memory",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P4.1",
            "P4.2"
          ],
          "description": "Tests: (a) local fails all attempts → escalation to frontier succeeds → returns frontier result, (b) no frontier available → error with remediation hint, (c) escalation tracking writes to memory on escalation, (d) escalation tracking writes success on local pass, (e) hex inference escalation-report reads tracking data. Use MockInferencePort for both local (fails) and frontier (succeeds).",
          "files": [
            "hex-nexus/tests/scaffolding.rs"
          ],
          "id": "P4.3",
          "layer": "primary",
          "name": "Escalation tests",
          "status": "done"
        }
      ],
      "tier": 3
    },
    {
      "description": "Wire ScaffoldedDispatch into the workplan executor, run end-to-end, document the feature.",
      "gate": {
        "blocking": true,
        "command": "cargo test --workspace && hex analyze .",
        "type": "test"
      },
      "id": "P5",
      "name": "Integration + documentation",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "In the workplan executor's dispatch path, wrap the IInferencePort adapter in ScaffoldedDispatch when the task tier is T1, T2, or T2.5. T3 tasks bypass scaffolding and go directly to frontier. The scaffolding layer is transparent to the executor — it just sees IInferencePort::complete() returning a result (which may have been the 5th attempt internally).",
          "files": [
            "hex-nexus/src/orchestration/workplan_executor.rs"
          ],
          "id": "P5.1",
          "layer": "primary",
          "name": "Wire ScaffoldedDispatch into workplan executor",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P5.1"
          ],
          "description": "Integration test: construct a T2 workplan task, boot ScaffoldedDispatch with MockInferencePort that fails on first attempt, succeeds on second. Assert: (a) two attempts made, (b) compile gate checked both, (c) second attempt's code returned, (d) escalation NOT triggered, (e) HexFlo memory records success.",
          "files": [
            "hex-nexus/tests/tiered_routing_e2e.rs"
          ],
          "id": "P5.2",
          "layer": "primary",
          "name": "End-to-end test: T2 task with mock Ollama",
          "status": "done"
        },
        {
          "agent": "hex-coder",
          "deps": [
            "P5.2"
          ],
          "description": "Add section on tiered inference routing to CLAUDE.md (under Inference) and README.md (under Running hex Standalone). Cover: tier mapping, how to configure via .hex/project.json, how to check escalation rates, how to override tier for a specific task. Keep it 10-15 lines per file.",
          "files": [
            "CLAUDE.md",
            "README.md"
          ],
          "id": "P5.3",
          "layer": "primary",
          "name": "Document tiered routing in CLAUDE.md + README.md",
          "status": "done"
        }
      ],
      "tier": 4
    }
  ],
  "priority": "high",
  "relates_to": "wp-hex-standalone-dispatch",
  "status": "done"
}