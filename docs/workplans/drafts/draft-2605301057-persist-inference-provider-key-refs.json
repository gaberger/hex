{
  "adr": "ADR-2026-04-11-0227",
  "created_at": "2026-05-30T10:57:52.773109425-04:00",
  "id": "draft-2605301057-persist-inference-provider-key-refs",
  "kind": "workplan-draft",
  "next_steps": [
    "Run /hex-feature-dev to expand this draft into a full workplan",
    "Or run `hex plan drafts approve draft-2605301057-persist-inference-provider-key-refs.json`",
    "Or run `hex plan drafts clear --name draft-2605301057-persist-inference-provider-key-refs`"
  ],
  "notes": "This is a draft created by ADR-2026-04-11-0227 auto-invoke. It contains only the original prompt — no specs, steps, or tiers have been generated yet. The planner agent will fill these in when the draft is picked up.",
  "origin": "auto-invoke",
  "prompt": "Persist inference provider key refs + calibration across nexus restart, and hot-reload tier_models without restart. Two linked gaps surfaced 2026-05-30 wiring Tenstorrent: (1) config_sync (ADR-044) re-syncs providers from a key-less cache on startup, wiping api_key_ref (requiresAuth->false), quantization (->q4) and qualityScore (->0.0) — every restart breaks vault-keyed cloud providers with 401 until manual re-registration; the secret_vault api_key_ref and saved calibration must survive restart. (2) TierModelConfig is loaded once at composition-root startup into InferenceRouterAdapter, so changes to .hex/project.json inference.tier_models require a full nexus restart to take effect — add a hot-reload path (e.g. extend /api/config/sync to rebuild the router's tier_config in place, or re-read per-request) so operators can swap tier models live. Also fix the inference test/health probe (GET /v1/models) which does not resolve the vault key like the chat path does.",
  "status": "pending-planner"
}