{
  "adr": "ADR-2604132330",
  "created_at": "2026-04-13T23:35:00Z",
  "created_by": "human+agent",
  "description": "Brain daemon drains a task queue each tick. Users/agents enqueue work via hex brain enqueue. Task types: hex-command, workplan, shell. SpacetimeDB-backed for persistence.",
  "feature": "Brain Inbox — Task Queue for Supervisor Loop",
  "id": "wp-brain-inbox",
  "phases": [
    {
      "description": "Use HexFlo memory with brain-task: key prefix — no new SpacetimeDB reducer needed, reuses existing infrastructure.",
      "gate": {
        "blocking": true,
        "command": "cargo check --workspace",
        "type": "test"
      },
      "id": "P1",
      "name": "Brain task storage (HexFlo memory)",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [],
          "description": "In hex-cli/src/commands/brain.rs: add enqueue_brain_task(kind, payload), list_brain_tasks(status_filter), drain_brain_tasks(limit) — all use HexFlo memory with keys brain-task:{uuid}. Value is JSON with {kind, payload, status, created_at, completed_at, result}.",
          "files": [
            "hex-cli/src/commands/brain.rs"
          ],
          "id": "P1.1",
          "layer": "primary",
          "name": "Brain task enqueue/list/drain via HexFlo memory",
          "status": "done",
          "strategy_hint": "codegen"
        }
      ],
      "tier": 1
    },
    {
      "description": "hex brain enqueue <kind> <payload>, hex brain queue list/clear/drain.",
      "gate": {
        "blocking": true,
        "command": "cargo check --workspace",
        "type": "test"
      },
      "id": "P2",
      "name": "CLI commands: enqueue, queue list/clear/drain",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [
            "P1.1"
          ],
          "description": "Add BrainAction::Enqueue { kind: String, payload: String } and BrainAction::Queue { #[command(subcommand)] action: QueueAction }. QueueAction: List, Clear, Drain. Wire into run() match.",
          "files": [
            "hex-cli/src/commands/brain.rs"
          ],
          "id": "P2.1",
          "layer": "primary",
          "name": "Add Enqueue and Queue subcommands to BrainAction",
          "status": "done",
          "strategy_hint": "codegen"
        }
      ],
      "tier": 2
    },
    {
      "description": "Each daemon tick: after validate, fetch up to N pending tasks and execute them sequentially.",
      "gate": {
        "blocking": true,
        "command": "cargo check --workspace",
        "type": "test"
      },
      "id": "P3",
      "name": "Daemon drains queue each tick",
      "tasks": [
        {
          "agent": "hex-coder",
          "deps": [
            "P1.1",
            "P2.1"
          ],
          "description": "In daemon() tick: after validate(), call drain_brain_tasks(limit=1). For each task, execute based on kind: hex-command=shell 'hex {payload}', workplan=shell 'hex plan execute {payload}', shell=whitelist check + execute. Set status=completed/failed with result, write back to HexFlo memory. Print task progress to stdout.",
          "files": [
            "hex-cli/src/commands/brain.rs"
          ],
          "id": "P3.1",
          "layer": "primary",
          "name": "Daemon tick body: drain brain queue",
          "status": "done",
          "strategy_hint": "codegen"
        }
      ],
      "tier": 2
    }
  ],
  "priority": "high",
  "status": "planned"
}