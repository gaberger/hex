{
  "adr": "ADR-2026-04-05-0900",
  "created_at": "2026-04-17T00:00:00Z",
  "description": "Fix 3 compiler warnings discovered in test run: unreachable code in plan/mod.rs, unused variable in worktree.rs, and unused function in sched.rs",
  "name": "wp-compiler-warnings-cleanup",
  "phases": [
    {
      "description": "Remove unreachable Ok(()) at line 532; refactor match arms or simplify control flow",
      "number": 1,
      "tasks": [
        {
          "description": "Match expression at line 405 diverges all arms; line 532 Ok(()) is unreachable. Refactor to either remove dead code or restructure match.",
          "files": [
            "hex-cli/src/commands/plan/mod.rs"
          ],
          "id": "P1-1",
          "status": "done",
          "strategy_hint": "codegen",
          "title": "Fix unreachable code in hex-cli/src/commands/plan/mod.rs:532"
        }
      ],
      "title": "Fix unreachable code in plan/mod.rs"
    },
    {
      "description": "Prefix unused `path` parameter with underscore or remove if genuinely unused",
      "number": 2,
      "tasks": [
        {
          "description": "Variable `path` in filter closure is unused. Either use it or prefix with underscore to suppress warning.",
          "files": [
            "hex-cli/src/commands/worktree.rs"
          ],
          "id": "P2-1",
          "status": "done",
          "strategy_hint": "codegen",
          "title": "Fix unused `path` variable in hex-cli/src/commands/worktree.rs:595"
        }
      ],
      "title": "Fix unused variable in worktree.rs"
    },
    {
      "description": "Delete notify_brain_task_result or integrate into call chain",
      "number": 3,
      "tasks": [
        {
          "description": "Function is never called; likely incomplete refactoring from brain queue work. Delete unless it should be wired into the brain task flow.",
          "files": [
            "hex-cli/src/commands/sched.rs"
          ],
          "id": "P3-1",
          "strategy_hint": "codegen",
          "title": "Remove unused notify_brain_task_result in hex-cli/src/commands/sched.rs:2359"
        }
      ],
      "title": "Remove unused function in sched.rs"
    },
    {
      "description": "Run cargo test --lib to confirm warnings are resolved",
      "number": 4,
      "tasks": [
        {
          "description": "Run 'cargo test --lib' and confirm all 3 warnings are gone and tests still pass",
          "files": [
            "hex-cli/src/commands/plan/mod.rs"
          ],
          "id": "P4-1",
          "strategy_hint": "codegen",
          "title": "Verify compilation succeeds with no warnings"
        }
      ],
      "title": "Verify and commit"
    }
  ],
  "priority": "P2",
  "title": "Clean compiler warnings from test run"
}