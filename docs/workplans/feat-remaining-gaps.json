{
  "id": "feat-remaining-gaps",
  "adr": "ADR-2604102100",
  "description": "Complete remaining gaps to achieve 100% feature parity with Anthropic Managed Agents",
  "feature": "Complete Managed Agents Parity",
  "dependsOn": ["feat-managed-agents-autonomy"],
  "phases": [
    {
      "id": "P0",
      "name": "Complete Docker Worker Guard",
      "dependsOn": [],
      "gate": {
        "gate_type": "build",
        "command": "cargo check --package hex-cli",
        "blocking": true
      },
      "tasks": [
        {
          "id": "T0.1",
          "name": "Remove if false && guard in docker worker",
          "layer": "adapters/secondary",
          "description": "Remove the conditional guard that disables Docker worker delegation (ADR-2603291900 P4)",
          "files": ["hex-cli/src/pipeline/supervisor.rs"],
          "done_condition": "cargo check --workspace",
          "done_command": "cargo check --workspace | grep -v warning",
          "deps": []
        }
      ]
    },
    {
      "id": "P1",
      "name": "Add Steering API",
      "dependsOn": ["P0"],
      "gate": {
        "gate_type": "build",
        "command": "cargo check --package hex-nexus",
        "blocking": true
      },
      "tasks": [
        {
          "id": "T1.1",
          "name": "Add POST /api/sessions/{id}/events endpoint",
          "layer": "adapters/primary",
          "description": "Add endpoint to send events to a running agent session",
          "files": ["hex-nexus/src/routes/orchestration.rs"],
          "done_condition": "cargo check --package hex-nexus",
          "done_command": "curl -s GET http://localhost:5555/openapi.json | grep sessions.*events",
          "deps": ["T0.1"]
        },
        {
          "id": "T1.2",
          "name": "Add POST /api/sessions/{id}/interrupt endpoint",
          "layer": "adapters/primary",
          "description": "Add endpoint to interrupt and restart a running agent session",
          "files": ["hex-nexus/src/routes/orchestration.rs"],
          "done_condition": "cargo check --package hex-nexus",
          "done_command": "curl -s GET http://localhost:5555/openapi.json | grep interrupt",
          "deps": ["T1.1"]
        }
      ]
    },
    {
      "id": "P2",
      "name": "Add Environment CRUD API",
      "dependsOn": ["P1"],
      "gate": {
        "gate_type": "build",
        "command": "cargo check --package hex-nexus",
        "blocking": true
      },
      "tasks": [
        {
          "id": "T2.1",
          "name": "Add POST/GET /api/environments endpoint",
          "layer": "adapters/primary",
          "description": "Add HTTP API to create/query container environments",
          "files": ["hex-nexus/src/routes/orchestration.rs"],
          "done_condition": "cargo check --package hex-nexus",
          "done_command": "curl -s GET http://localhost:5555/openapi.json | grep environments",
          "deps": ["T1.2"]
        }
      ]
    },
    {
      "id": "P3",
      "name": "Add Session CRUD API",
      "dependsOn": ["P2"],
      "gate": {
        "gate_type": "build",
        "command": "cargo check --package hex-nexus",
        "blocking": true
      },
      "tasks": [
        {
          "id": "T3.1",
          "name": "Add POST/GET /api/sessions endpoint",
          "layer": "adapters/primary",
          "description": "Add HTTP API to create/query agent sessions",
          "files": ["hex-nexus/src/routes/orchestration.rs"],
          "done_condition": "cargo check --package hex-nexus",
          "done_command": "curl -s GET http://localhost:5555/openapi.json | grep '\"(/api/sessions'",
          "deps": ["T2.1"]
        }
      ]
    },
    {
      "id": "P4",
      "name": "Integration Test",
      "dependsOn": ["P3"],
      "gate": {
        "gate_type": "test",
        "command": "cargo test --package hex-cli -- workplan",
        "blocking": true
      },
      "tasks": [
        {
          "id": "T4.1",
          "name": "Verify all new endpoints work",
          "layer": "integration",
          "description": "Test all new endpoints and verify feature parity",
          "files": [],
          "done_condition": "hex analyze . | grep 'Architecture grade: A'",
          "done_command": "hex analyze .",
          "deps": ["T3.1"]
        }
      ]
    }
  ],
  "status": "planned",
  "successCriteria": [
    "Docker worker guard removed (P0c complete)",
    "Steering API works (P3a/b complete)",
    "Environment API works (P5a complete)",
    "Session API works (P5b complete)",
    "hex analyze shows A+ grade"
  ]
}