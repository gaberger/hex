{
  "name": "@anthropic-hex/hex",
  "version": "26.8.0",
  "description": "Hexagonal Architecture framework for LLM-driven development with token-efficient tree-sitter summaries",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "bin": {
    "hex": "dist/cli.js"
  },
  "scripts": {
    "build:native": "bash scripts/build-hex-core.sh",
    "build": "bun build src/cli.ts --outfile dist/cli.js --target node --format esm && bun build src/index.ts --outfile dist/index.js --target node --format esm && cp node_modules/web-tree-sitter/tree-sitter.wasm dist/tree-sitter.wasm 2>/dev/null || true",
    "build:all": "bun run build:native; bun run build",
    "build:types": "tsc --emitDeclarationOnly --outDir dist",
    "test": "bun test $(find tests/unit tests/property tests/smoke -name '*.test.ts' ! -name 'dashboard-adapter.test.ts' ! -name 'hub-launcher.test.ts' ! -name 'local-vault-adapter.test.ts' ! -name 'cli-secrets-vault.test.ts' 2>/dev/null) && bun test tests/unit/local-vault-adapter.test.ts && bun test tests/unit/cli-secrets-vault.test.ts && bun test tests/unit/dashboard-adapter.test.ts && bun test tests/unit/hub-launcher.test.ts && bun test tests/integration/",
    "test:unit": "bun test tests/unit/",
    "test:property": "bun test tests/property/",
    "test:smoke": "bun test tests/smoke/",
    "test:integration": "bun test tests/integration/",
    "test:e2e": "bun test tests/e2e/",
    "test:all": "bun test $(find tests/unit tests/property tests/smoke -name '*.test.ts' ! -name 'dashboard-adapter.test.ts' ! -name 'hub-launcher.test.ts' 2>/dev/null) && bun test tests/unit/dashboard-adapter.test.ts && bun test tests/unit/hub-launcher.test.ts && bun test tests/integration/ && bun test tests/e2e/",
    "test:watch": "bun test --watch",
    "lint": "eslint src/ tests/ --ext .ts",
    "lint:fix": "eslint src/ tests/ --ext .ts --fix",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "bun run --watch src/cli.ts",
    "start": "bun src/cli.ts",
    "prepublishOnly": "bun run clean && bun run build && bun run build:types && bun test"
  },
  "keywords": [
    "hexagonal-architecture",
    "ports-and-adapters",
    "llm",
    "tree-sitter",
    "code-generation",
    "ai-development",
    "swarm"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anthropic-hex/hex.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist",
    "skills",
    "agents",
    "config",
    "scripts"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/ws": "^8.18.1",
    "@typescript-eslint/eslint-plugin": "^8.57.0",
    "@typescript-eslint/parser": "^8.57.0",
    "eslint": "^10.0.3",
    "fast-check": "^4.1.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@claude-flow/cli": "3.5.36",
    "agentdb": "^3.0.0-alpha.10",
    "esbuild": "^0.27.4",
    "flatted": "^3.4.2",
    "tree-sitter-wasms": "^0.1.13",
    "web-tree-sitter": "^0.26.7",
    "ws": "^8.19.0"
  }
}
