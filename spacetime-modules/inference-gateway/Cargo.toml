[package]
name = "inference-gateway"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "1.0", features = ["unstable"] }
log = "0.4"
serde_json = { version = "1", default-features = false, features = ["alloc"] }
